import React from 'react'

const Estacionados = () => {

    const params = useParams();
    
    
    
    
    
    
    
    const catElegida = (prods.filter(x => x.id === id))[0];

  return (
    <div>Estacionados</div>
  )
}

export default Estacionados